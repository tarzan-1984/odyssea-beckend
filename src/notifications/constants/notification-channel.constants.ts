/**
 * Android notification channel ID
 * Must match the channel ID used in mobile app (Expo)
 */
export const ANDROID_NOTIFICATION_CHANNEL_ID = 'odysseia-messages';

// Android notification icon name (resource name in drawable, without extension)
// This should match the icon file name in app.json (notification-icon.png -> notification_icon)
export const ANDROID_NOTIFICATION_ICON = 'notification_icon';
